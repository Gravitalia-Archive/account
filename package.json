{
  "name": "account",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "wrangler pages dev ./dist",
    "astro": "astro",
    "deploy": "npm run build && npx wrangler pages publish dist --project-name account --branch main"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^6.2.4",
    "@astrojs/prefetch": "^0.2.1",
    "@astrojs/sitemap": "^1.3.0",
    "@astrojs/tailwind": "^3.1.2",
    "astro": "^2.4.2",
    "tailwindcss": "^3.3.2"
  }
}
